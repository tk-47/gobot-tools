# Apple Health — Webhook Secret (REQUIRED)
#
# A secret token that Health Auto Export sends with every request.
# The bot rejects any request missing this header — endpoint is disabled without it.
#
# Generate a strong random secret:
#   openssl rand -hex 32
#
# Then add the same value in Health Auto Export:
#   Automation → your webhook → Headers → x-webhook-secret: <your-secret>

APPLE_HEALTH_WEBHOOK_SECRET=your_secret_here

# Your local timezone for sleep time display (default: America/Chicago)
# USER_TIMEZONE=America/New_York
